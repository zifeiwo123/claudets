请审查当前 `report/` 目录和报告生成逻辑。

重点检查：

1. `summary.json`、`iteration_summary.csv`、`report_v*.md`、图表数字是否一致。
2. 报告是否写清数据源、复权状态、日线特征快照、股票池、成本、是否做空。
3. 是否只展示最好 iteration，而没有展示整体分布。
4. 是否把未实现功能写成已实现。
5. 是否把 long-short 因子研究组合写成普通 A 股 long-only 实盘结果。
6. 因子表达式是否可复现，是否显示窗口参数。
7. 旧报告是否仍可用；若代码口径已变，必须标记旧报告不可作为新结论。

先不要修改代码，先给审查结论和建议报告结构。
