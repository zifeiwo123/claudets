# Codex / Claude Handoff

This folder is the durable handoff lane between Codex and Claude for the
`claudets` project.

Every agent that changes the repo should update this file before handoff:

1. Add what was changed.
2. Add what was verified.
3. Add what remains unsafe or preliminary.
4. Add the next request for the other agent.

Keep this file ASCII-safe unless the user explicitly asks for Chinese text in
the file. The repo has had encoding drift before, so plain ASCII is preferred.

---

## Standing Rules

- Do not restart GP unless the user explicitly asks after an audit.
- Do not enter Phase 2 while the final holdout remains only a preliminary pass.
- Do not edit source data parquet/db files.
- Reports must be generated from a single result source whenever possible.
- Do not manually paste strategy, benchmark, excess, NAV, turnover, or cost
  numbers from separate runs into the same report.
- For weekly strategies, state whether a date is `signal_date` or
  `return_end_date`.
- Use `preliminary pass` unless all metrics are single-source reproducible and
  residual report-generation risks are resolved.
- Keep new governance docs ASCII-safe to avoid mojibake.

---

## Current State

Latest pushed commit at the time of this note:

```text
8b08222 Clarify final holdout return dates
```

Final holdout status:

```text
Conclusion: preliminary pass
GP: paused
Phase 2: paused
```

Reproducible final holdout command:

```powershell
python diagnostics\final_holdout_audit.py
```

If `python` resolves to the Windows Store placeholder, use the installed Python
3.11 executable directly:

```powershell
& "C:\Users\liuji\AppData\Local\Microsoft\WindowsApps\PythonSoftwareFoundation.Python.3.11_qbz5n2kfra8p0\python.exe" diagnostics\final_holdout_audit.py
```

Key generated files:

```text
report/final_holdout_weekly_detail.parquet
report/final_holdout_metrics.json
report/final_holdout_volume_u3_lo50.md
report/final_holdout_audit.md
analysis/final_holdout_volume_u3_lo50.md
analysis/final_holdout_audit.md
```

Note: parquet/json files under `report/` may be ignored by git. They should be
regenerated by the script when needed.

---

## What Codex Did

### 2026-05-13 - Final holdout benchmark audit

Commit:

```text
f9cfdb3 Audit final holdout benchmark metrics
```

Codex found that the old final holdout report mixed inconsistent benchmark
metrics:

- Strategy annualized return was reproducible at about +12.9%.
- Annualized excess return was reproducible at about +3.4%.
- Universe EW annualized return was incorrectly reported as -31.6%.
- Recomputed Universe EW annualized return was about +8.7%.

Actions:

- Downgraded final holdout from `PASS` to `preliminary pass`.
- Added `report/final_holdout_audit.md`.
- Added `skills/claudets-research-governance/` to encode project governance.
- Kept GP and Phase 2 paused.

Validation:

- Recomputed from stored holdout parquet.
- Independently rebuilt U3 / -volume / LO50 from `weekly_daily_features.parquet`.
- Ran compile checks and skill validation.

### 2026-05-13 - Final holdout reproducibility review

Claude created:

```text
9fe05c2 Make final holdout audit reproducible
```

Codex reviewed and then added:

```text
8b08222 Clarify final holdout return dates
```

Actions:

- Made `diagnostics/final_holdout_audit.py` runnable directly from repo root.
- Added `return_end_date` to final holdout weekly detail and reports.
- Replaced mojibake / non-ASCII dash artifacts in generated audit docs.
- Regenerated report and analysis final holdout docs.

Validation:

- Ran `diagnostics/final_holdout_audit.py`.
- Ran `python -m compileall diagnostics`.
- Ran `python -m compileall .`.
- Checked no final holdout report claims final PASS or cleared Phase 2.

---

## What Claude Did

### 2026-05-13 - Final holdout monitoring runbook

Commit (expected after this handoff update):

```text
Add final holdout monitoring runbook
```

Actions:

- Added `docs/final_holdout_monitoring_runbook.md`: complete runbook covering
  required data, regenerate command, output files, date conventions, primary and
  secondary metrics, downgrade triggers, evidence needed for final PASS, and
  cross-references to related documents.
- Added `diagnostics/final_holdout_monitor_check.py`: lightweight integrity
  check that loads `report/final_holdout_metrics.json` and verifies consistency
  without recomputing. Exit code 0 = status unchanged; exit code 1 = broken.
- Updated `codexmd/CODEX_CLAUDE_HANDOFF.md` (this section).

Validation:

- `python -m compileall diagnostics`: passed
- `python -m diagnostics.final_holdout_audit`: regenerated all reports cleanly
- `python diagnostics/final_holdout_monitor_check.py`: OK, status consistent
- `git diff --check`: no whitespace issues
- Checked touched docs: all ASCII-safe, no mojibake

GP and Phase 2 remained paused throughout.

---

## Request For Codex

Codex should do this next, unless the user redirects:

```text
Do not restart GP. Do not enter Phase 2.

Please review the runbook and check script for correctness:

1. Verify the regenerate command in the runbook works from a fresh checkout.
2. Verify the monitor check catches a broken metrics.json (e.g., manually edit
   ir_vs_ew to -0.1 and confirm exit code 1).
3. Verify the runbook date convention explanation matches the actual parquet
   columns (signal_date, return_end_date).
4. If anything is wrong, fix it and update this file.
5. If everything is correct, confirm in this file.

After review, keep GP paused and Phase 2 paused.
```

---

## Open Risks

- Final holdout has only 13 weekly observations.
- Walk-forward showed failures in 2023 regimes.
- A clean short holdout is not enough to promote to final PASS.
- Report-generation is now scripted, but future agents must avoid editing the
  generated report numbers manually.
- Some older project docs still contain stale Phase 2 / GP language and should
  not be treated as the current decision state.
